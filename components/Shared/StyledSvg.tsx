import { styled } from "@mui/material/styles";

const StyledSvg = styled("svg")(({ theme }) => ({
  width: "3rem",
  height: "3rem",
  position: "absolute",
  borderRadius: "5px",
}));

export default StyledSvg;
